// Purpose: Identify IPs failing credentials against many distinct accounts.
// ResultType 50126 represents invalid username or password.
CloudoraSignIn_CL
| where ResultType == "50126"
| summarize Failures=count(), TargetedAccounts=dcount(UserPrincipalName) by IPAddress, Country
| order by TargetedAccounts desc, Failures desc
