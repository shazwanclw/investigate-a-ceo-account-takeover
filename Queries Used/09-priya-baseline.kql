// Purpose: Test whether Priya's attacker-associated activity fits her history.
CloudoraSignIn_CL
| where UserPrincipalName == "priya.nair@cloudora.io"
| summarize SignInCount=count(), FirstSeen=min(TimeGenerated), LastSeen=max(TimeGenerated) by Country, City, IPAddress
| order by SignInCount desc
